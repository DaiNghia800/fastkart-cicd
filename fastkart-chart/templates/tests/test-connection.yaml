apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastkart-chart.fullname" . }}-test-connection"
  labels:
    {{- include "fastkart-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastkart-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
