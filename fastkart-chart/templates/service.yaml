apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastkart-chart.fullname" . }}
  labels:
    {{- include "fastkart-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastkart-chart.selectorLabels" . | nindent 4 }}
